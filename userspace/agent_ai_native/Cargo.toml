[package]
name = "agent_ai_native"
version = "0.1.0"
edition = "2021"
authors = ["MORNINGSTAR <morningstar@aetherion.dev>"]
description = "AetherionOS Jalon 34 - Native Rust no_std Tensor Engine with Hardware SSE2 intrinsics"

[dependencies]
aetherion_sdk = { path = "../rust_sdk" }

[profile.release]
opt-level = 2
lto = false
panic = "abort"
